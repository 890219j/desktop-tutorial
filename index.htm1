<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>智慧健康管理系統</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@300;400;500;700;900&display=swap" rel="stylesheet">
<style>
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --primary: #1a73e8;
  --primary-dark: #1557b0;
  --primary-light: #e8f0fe;
  --accent: #00bcd4;
  --accent-dark: #0097a7;
  --success: #34a853;
  --warning: #fbbc04;
  --danger: #ea4335;
  --gray-50: #f8f9fa;
  --gray-100: #f1f3f4;
  --gray-200: #e8eaed;
  --gray-300: #dadce0;
  --gray-400: #bdc1c6;
  --gray-500: #9aa0a6;
  --gray-600: #80868b;
  --gray-700: #5f6368;
  --gray-800: #3c4043;
  --gray-900: #202124;
  --shadow-sm: 0 1px 2px rgba(0,0,0,.06), 0 1px 3px rgba(0,0,0,.1);
  --shadow-md: 0 4px 6px rgba(0,0,0,.07), 0 2px 4px rgba(0,0,0,.06);
  --shadow-lg: 0 10px 15px rgba(0,0,0,.1), 0 4px 6px rgba(0,0,0,.05);
  --shadow-xl: 0 20px 25px rgba(0,0,0,.1), 0 10px 10px rgba(0,0,0,.04);
  --radius: 12px;
  --radius-sm: 8px;
  --radius-lg: 16px;
  --transition: .2s cubic-bezier(.4,0,.2,1);
}

html { scroll-behavior: smooth; }

body {
  font-family: 'Noto Sans TC', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  background: var(--gray-50);
  color: var(--gray-900);
  line-height: 1.6;
  min-height: 100vh;
}

/* ===== LOGIN SCREEN ===== */
.login-screen {
  position: fixed;
  inset: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #0d47a1 0%, #1565c0 30%, #1976d2 60%, #00838f 100%);
  transition: opacity .6s ease, visibility .6s ease;
}

.login-screen.hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.login-box {
  background: rgba(255,255,255,.95);
  backdrop-filter: blur(20px);
  border-radius: var(--radius-lg);
  padding: 48px 40px 40px;
  width: 400px;
  max-width: 92vw;
  text-align: center;
  box-shadow: 0 24px 80px rgba(0,0,0,.3);
  position: relative;
  overflow: hidden;
}

.login-box::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, var(--primary), var(--accent));
}

.login-icon {
  width: 72px;
  height: 72px;
  margin: 0 auto 16px;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32px;
  color: #fff;
  box-shadow: 0 8px 24px rgba(26,115,232,.3);
}

.login-box h2 {
  font-size: 22px;
  font-weight: 700;
  margin-bottom: 4px;
  color: var(--gray-900);
}

.login-box p {
  font-size: 14px;
  color: var(--gray-600);
  margin-bottom: 28px;
}

.input-group {
  margin-bottom: 18px;
  text-align: left;
}

.input-group label {
  display: block;
  font-size: 13px;
  font-weight: 500;
  color: var(--gray-700);
  margin-bottom: 6px;
}

.input-wrapper {
  display: flex;
  align-items: center;
  gap: 10px;
  background: var(--gray-100);
  border-radius: var(--radius-sm);
  padding: 0 14px;
  border: 2px solid transparent;
  transition: border-color var(--transition), background var(--transition);
}

.input-wrapper:focus-within {
  border-color: var(--primary);
  background: #fff;
}

.input-wrapper .icon {
  font-size: 18px;
  opacity: .5;
}

.input-wrapper input {
  flex: 1;
  border: none;
  background: transparent;
  padding: 12px 0;
  font-size: 15px;
  font-family: inherit;
  outline: none;
  color: var(--gray-900);
}

.input-wrapper input::placeholder {
  color: var(--gray-400);
}

.login-btn {
  width: 100%;
  padding: 13px;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 16px;
  font-weight: 700;
  font-family: inherit;
  cursor: pointer;
  transition: transform var(--transition), box-shadow var(--transition);
  margin-top: 8px;
  letter-spacing: 1px;
}

.login-btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 8px 24px rgba(26,115,232,.35);
}

.login-btn:active {
  transform: translateY(0);
}

.login-footer {
  margin-top: 20px;
  font-size: 12px;
  color: var(--gray-500);
}

/* ===== MAIN DASHBOARD ===== */
.app {
  display: none;
}

.app.active {
  display: block;
}

/* Sidebar */
.sidebar {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: 260px;
  background: #fff;
  border-right: 1px solid var(--gray-200);
  z-index: 100;
  display: flex;
  flex-direction: column;
  transition: transform var(--transition);
}

.sidebar-brand {
  padding: 24px 20px 20px;
  display: flex;
  align-items: center;
  gap: 12px;
  border-bottom: 1px solid var(--gray-200);
}

.sidebar-brand .logo {
  width: 40px;
  height: 40px;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 18px;
  font-weight: 900;
}

.sidebar-brand .brand-text {
  font-size: 16px;
  font-weight: 700;
  color: var(--gray-900);
}

.sidebar-brand .brand-text small {
  display: block;
  font-size: 11px;
  font-weight: 400;
  color: var(--gray-500);
}

.sidebar-nav {
  flex: 1;
  padding: 12px;
  overflow-y: auto;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 14px;
  border-radius: var(--radius-sm);
  color: var(--gray-700);
  text-decoration: none;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition);
  margin-bottom: 2px;
}

.nav-item:hover {
  background: var(--gray-100);
  color: var(--gray-900);
}

.nav-item.active {
  background: var(--primary-light);
  color: var(--primary);
  font-weight: 700;
}

.nav-item .nav-icon {
  font-size: 20px;
  width: 24px;
  text-align: center;
}

.nav-item .badge {
  margin-left: auto;
  background: var(--danger);
  color: #fff;
  font-size: 11px;
  font-weight: 700;
  padding: 2px 8px;
  border-radius: 10px;
  min-width: 20px;
  text-align: center;
}

.sidebar-footer {
  padding: 16px 20px;
  border-top: 1px solid var(--gray-200);
  display: flex;
  align-items: center;
  gap: 10px;
}

.sidebar-footer .avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 14px;
  font-weight: 700;
}

.sidebar-footer .user-info {
  flex: 1;
  font-size: 13px;
  font-weight: 500;
  color: var(--gray-800);
}

.sidebar-footer .user-info small {
  display: block;
  font-size: 11px;
  font-weight: 400;
  color: var(--gray-500);
}

/* Main Content */
.main-content {
  margin-left: 260px;
  padding: 24px 32px;
  min-height: 100vh;
}

/* Top Bar */
.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24px;
  gap: 16px;
}

.topbar-left h1 {
  font-size: 24px;
  font-weight: 700;
  color: var(--gray-900);
}

.topbar-left p {
  font-size: 14px;
  color: var(--gray-600);
  margin-top: 2px;
}

.topbar-right {
  display: flex;
  align-items: center;
  gap: 12px;
}

.topbar-right .date-display {
  font-size: 14px;
  color: var(--gray-600);
  padding: 8px 16px;
  background: #fff;
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-sm);
}

.icon-btn {
  width: 40px;
  height: 40px;
  border: none;
  background: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  cursor: pointer;
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--transition), transform var(--transition);
  position: relative;
}

.icon-btn:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);
}

.icon-btn .dot {
  position: absolute;
  top: 8px;
  right: 8px;
  width: 8px;
  height: 8px;
  background: var(--danger);
  border-radius: 50%;
  border: 2px solid #fff;
}

/* Stats Grid */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin-bottom: 24px;
}

.stat-card {
  background: #fff;
  border-radius: var(--radius);
  padding: 20px 24px;
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--transition), transform var(--transition);
}

.stat-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

.stat-card .stat-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

.stat-card .stat-icon {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22px;
}

.stat-card .stat-label {
  font-size: 13px;
  color: var(--gray-600);
  font-weight: 500;
}

.stat-card .stat-value {
  font-size: 28px;
  font-weight: 900;
  color: var(--gray-900);
  line-height: 1.2;
}

.stat-card .stat-change {
  font-size: 12px;
  font-weight: 500;
  margin-top: 4px;
}

.stat-card .stat-change.up { color: var(--success); }
.stat-card .stat-change.down { color: var(--danger); }

/* Content Grid */
.content-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-bottom: 24px;
}

.card {
  background: #fff;
  border-radius: var(--radius);
  box-shadow: var(--shadow-sm);
  overflow: hidden;
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 24px;
  border-bottom: 1px solid var(--gray-200);
}

.card-header h3 {
  font-size: 16px;
  font-weight: 700;
  color: var(--gray-900);
}

.card-header .card-action {
  font-size: 13px;
  color: var(--primary);
  cursor: pointer;
  font-weight: 500;
  transition: color var(--transition);
}

.card-header .card-action:hover {
  color: var(--primary-dark);
}

.card-body {
  padding: 16px 24px 20px;
}

/* Timeline / Schedule */
.schedule-item {
  display: flex;
  gap: 14px;
  padding: 12px 0;
  border-bottom: 1px solid var(--gray-100);
}

.schedule-item:last-child {
  border-bottom: none;
}

.schedule-time {
  min-width: 52px;
  text-align: right;
  font-size: 13px;
  font-weight: 600;
  color: var(--gray-600);
  padding-top: 2px;
}

.schedule-dot-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
}

.schedule-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  border: 2px solid var(--primary);
  background: #fff;
  flex-shrink: 0;
  margin-top: 4px;
}

.schedule-dot.taken {
  background: var(--success);
  border-color: var(--success);
}

.schedule-dot.missed {
  background: var(--danger);
  border-color: var(--danger);
}

.schedule-dot.upcoming {
  background: #fff;
  border-color: var(--gray-400);
}

.schedule-line {
  width: 2px;
  flex: 1;
  background: var(--gray-200);
  min-height: 16px;
}

.schedule-info {
  flex: 1;
}

.schedule-info .med-name {
  font-size: 14px;
  font-weight: 600;
  color: var(--gray-900);
}

.schedule-info .med-desc {
  font-size: 12px;
  color: var(--gray-600);
  margin-top: 2px;
}

.schedule-info .med-status {
  display: inline-block;
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 4px;
  margin-top: 4px;
}

.med-status.taken {
  background: #e6f4ea;
  color: var(--success);
}

.med-status.missed {
  background: #fce8e6;
  color: var(--danger);
}

.med-status.upcoming {
  background: var(--gray-100);
  color: var(--gray-600);
}

/* Medicine List */
.medicine-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.med-card {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 14px;
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  transition: background var(--transition);
  cursor: pointer;
}

.med-card:hover {
  background: var(--gray-100);
}

.med-card .med-color {
  width: 4px;
  height: 36px;
  border-radius: 2px;
  flex-shrink: 0;
}

.med-card .med-info {
  flex: 1;
  min-width: 0;
}

.med-card .med-name {
  font-size: 13px;
  font-weight: 600;
  color: var(--gray-900);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.med-card .med-dosage {
  font-size: 11px;
  color: var(--gray-600);
}

.med-card .med-count {
  font-size: 12px;
  font-weight: 600;
  color: var(--gray-600);
  white-space: nowrap;
}

/* Device Status */
.device-status {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 16px;
  background: linear-gradient(135deg, var(--primary-light), #fff);
  border-radius: var(--radius-sm);
  margin-bottom: 16px;
}

.device-status .device-icon {
  width: 52px;
  height: 52px;
  background: #fff;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28px;
  box-shadow: var(--shadow-sm);
}

.device-status .device-info {
  flex: 1;
}

.device-status .device-name {
  font-size: 15px;
  font-weight: 600;
  color: var(--gray-900);
}

.device-status .device-desc {
  font-size: 12px;
  color: var(--gray-600);
}

.device-status .device-indicator {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  font-weight: 600;
}

.device-indicator .led {
  width: 10px;
  height: 10px;
  border-radius: 50%;
}

.device-indicator .led.online {
  background: var(--success);
  box-shadow: 0 0 8px rgba(52,168,83,.5);
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: .6; }
}

.device-indicator .led.offline {
  background: var(--gray-400);
}

.compartment-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 8px;
}

.compartment {
  text-align: center;
  padding: 12px 8px;
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  transition: all var(--transition);
  cursor: pointer;
}

.compartment:hover {
  background: var(--gray-100);
}

.compartment .comp-icon {
  font-size: 24px;
  margin-bottom: 4px;
}

.compartment .comp-label {
  font-size: 11px;
  font-weight: 600;
  color: var(--gray-700);
}

.compartment .comp-status {
  font-size: 10px;
  font-weight: 500;
}

.comp-status.full { color: var(--success); }
.comp-status.low { color: var(--warning); }
.comp-status.empty { color: var(--danger); }

/* Health Metrics */
.metrics-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  margin-bottom: 12px;
}

.metric {
  text-align: center;
  padding: 14px 8px;
  background: var(--gray-50);
  border-radius: var(--radius-sm);
}

.metric .metric-value {
  font-size: 22px;
  font-weight: 900;
  color: var(--primary);
}

.metric .metric-label {
  font-size: 11px;
  color: var(--gray-600);
  margin-top: 2px;
}

.weekly-chart {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  height: 80px;
  padding-top: 8px;
  gap: 4px;
}

.weekly-chart .bar-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

.weekly-chart .bar {
  width: 100%;
  max-width: 32px;
  border-radius: 4px 4px 0 0;
  background: linear-gradient(180deg, var(--primary), var(--accent));
  transition: height .6s ease;
  min-height: 4px;
}

.weekly-chart .bar-label {
  font-size: 10px;
  color: var(--gray-600);
  font-weight: 500;
}

/* Full width card */
.card.full-width {
  grid-column: 1 / -1;
}

/* Responsive */
.hamburger {
  display: none;
  width: 40px;
  height: 40px;
  border: none;
  background: #fff;
  border-radius: var(--radius-sm);
  font-size: 22px;
  cursor: pointer;
  box-shadow: var(--shadow-sm);
  align-items: center;
  justify-content: center;
}

.overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.4);
  z-index: 99;
}

@media (max-width: 1200px) {
  .stats-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 992px) {
  .content-grid { grid-template-columns: 1fr; }
  .medicine-grid { grid-template-columns: 1fr; }
  .metrics-row { grid-template-columns: repeat(3, 1fr); }
}

@media (max-width: 768px) {
  .sidebar {
    transform: translateX(-100%);
  }
  .sidebar.open {
    transform: translateX(0);
  }
  .overlay.show {
    display: block;
  }
  .main-content {
    margin-left: 0;
    padding: 16px;
  }
  .stats-grid { grid-template-columns: repeat(2, 1fr); }
  .hamburger { display: flex; }
  .topbar-left h1 { font-size: 20px; }
  .login-box { padding: 32px 24px 28px; }
  .compartment-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 480px) {
  .stats-grid { grid-template-columns: 1fr; }
  .metrics-row { grid-template-columns: 1fr; }
  .schedule-time { min-width: 40px; font-size: 12px; }
}
</style>
</head>
<body>

<!-- ========== LOGIN SCREEN ========== -->
<div class="login-screen" id="loginScreen">
  <div class="login-box">
    <div class="login-icon">+</div>
    <h2>智慧健康管理系統</h2>
    <p>請輸入您的帳號密碼以存取系統</p>
    <div class="input-group">
      <label>帳號</label>
      <div class="input-wrapper">
        <span class="icon">👤</span>
        <input type="text" id="username" placeholder="請輸入帳號" value="user@kutech">
      </div>
    </div>
    <div class="input-group">
      <label>密碼</label>
      <div class="input-wrapper">
        <span class="icon">🔒</span>
        <input type="password" id="password" placeholder="請輸入密碼" value="123456">
      </div>
    </div>
    <button class="login-btn" onclick="handleLogin()">登入系統</button>
    <div class="login-footer">Copyright &copy; 2025 智慧健康管理系統</div>
  </div>
</div>

<!-- ========== OVERLAY ========== -->
<div class="overlay" id="overlay" onclick="toggleSidebar()"></div>

<!-- ========== SIDEBAR ========== -->
<aside class="sidebar" id="sidebar">
  <div class="sidebar-brand">
    <div class="logo">+</div>
    <div class="brand-text">
      智慧健康
      <small>Smart Health Management</small>
    </div>
  </div>
  <nav class="sidebar-nav">
    <a class="nav-item active" onclick="navigate(this)">
      <span class="nav-icon">📊</span> 儀表板
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">💊</span> 用藥管理
      <span class="badge">3</span>
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">📅</span> 排程提醒
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">📋</span> 健康記錄
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">📦</span> 藥盒狀態
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">📈</span> 統計分析
    </a>
    <a class="nav-item" onclick="navigate(this)">
      <span class="nav-icon">⚙️</span> 系統設定
    </a>
  </nav>
  <div class="sidebar-footer">
    <div class="avatar">王</div>
    <div class="user-info">
      王大明
      <small>admin@kutech</small>
    </div>
    <div style="cursor:pointer;font-size:18px;opacity:.5" onclick="handleLogout()">🚪</div>
  </div>
</aside>

<!-- ========== MAIN CONTENT ========== -->
<main class="main-content" id="mainContent">
  <div class="topbar">
    <div class="topbar-left">
      <button class="hamburger" onclick="toggleSidebar()">☰</button>
      <h1>儀表板</h1>
      <p>歡迎回來，王大明！今天記得按時服藥喔</p>
    </div>
    <div class="topbar-right">
      <div class="date-display" id="dateDisplay"></div>
      <button class="icon-btn" onclick="handleNotification()">
        🔔
        <span class="dot"></span>
      </button>
    </div>
  </div>

  <!-- Stats -->
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-header">
        <div class="stat-icon" style="background:#e8f0fe;color:#1a73e8">💊</div>
        <span class="stat-label">今日用藥</span>
      </div>
      <div class="stat-value">6</div>
      <div class="stat-change up">已完成 4 劑</div>
    </div>
    <div class="stat-card">
      <div class="stat-header">
        <div class="stat-icon" style="background:#e6f4ea;color:#34a853">✅</div>
        <span class="stat-label">已服用</span>
      </div>
      <div class="stat-value">4</div>
      <div class="stat-change up">遵從率 80%</div>
    </div>
    <div class="stat-card">
      <div class="stat-header">
        <div class="stat-icon" style="background:#fce8e6;color:#ea4335">⏰</div>
        <span class="stat-label">待服用</span>
      </div>
      <div class="stat-value">2</div>
      <div class="stat-change down">下午 3:00 下次提醒</div>
    </div>
    <div class="stat-card">
      <div class="stat-header">
        <div class="stat-icon" style="background:#fef7e0;color:#fbbc04">📦</div>
        <span class="stat-label">藥盒存量</span>
      </div>
      <div class="stat-value">73%</div>
      <div class="stat-change down">2 個藥格即將用盡</div>
    </div>
  </div>

  <!-- Content Grid -->
  <div class="content-grid">
    <!-- Schedule -->
    <div class="card">
      <div class="card-header">
        <h3>今日用藥時程</h3>
        <span class="card-action">檢視全部 →</span>
      </div>
      <div class="card-body">
        <div class="schedule-item">
          <div class="schedule-time">08:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot taken"></div>
            <div class="schedule-line"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">諾華 降血壓藥 50mg</div>
            <div class="med-desc">早餐後服用 · 1 錠</div>
            <span class="med-status taken">已服用</span>
          </div>
        </div>
        <div class="schedule-item">
          <div class="schedule-time">08:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot taken"></div>
            <div class="schedule-line"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">拜瑞妥 抗凝血劑 20mg</div>
            <div class="med-desc">早餐後服用 · 1 錠</div>
            <span class="med-status taken">已服用</span>
          </div>
        </div>
        <div class="schedule-item">
          <div class="schedule-time">12:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot taken"></div>
            <div class="schedule-line"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">糖漸平 降血糖藥 500mg</div>
            <div class="med-desc">午餐前服用 · 1 錠</div>
            <span class="med-status taken">已服用</span>
          </div>
        </div>
        <div class="schedule-item">
          <div class="schedule-time">12:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot taken"></div>
            <div class="schedule-line"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">鈣穩 維生素D3 800IU</div>
            <div class="med-desc">午餐後服用 · 1 錠</div>
            <span class="med-status taken">已服用</span>
          </div>
        </div>
        <div class="schedule-item">
          <div class="schedule-time">15:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot upcoming"></div>
            <div class="schedule-line"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">立普妥 降血脂藥 10mg</div>
            <div class="med-desc">下午服用 · 1 錠</div>
            <span class="med-status upcoming">待服用</span>
          </div>
        </div>
        <div class="schedule-item">
          <div class="schedule-time">21:00</div>
          <div class="schedule-dot-wrapper">
            <div class="schedule-dot upcoming"></div>
          </div>
          <div class="schedule-info">
            <div class="med-name">悠樂丁 助眠藥 2mg</div>
            <div class="med-desc">睡前服用 · 0.5 錠</div>
            <span class="med-status upcoming">待服用</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Device Status -->
    <div class="card">
      <div class="card-header">
        <h3>智慧藥盒狀態</h3>
        <span class="card-action">詳細資訊 →</span>
      </div>
      <div class="card-body">
        <div class="device-status">
          <div class="device-icon">📦</div>
          <div class="device-info">
            <div class="device-name">Kutech 智慧藥盒 Pro</div>
            <div class="device-desc">韌體 v2.4.1 · 連線品質 優良</div>
          </div>
          <div class="device-indicator">
            <span class="led online"></span> 線上
          </div>
        </div>
        <div class="compartment-grid">
          <div class="compartment">
            <div class="comp-icon">🔴</div>
            <div class="comp-label">A1 降血壓</div>
            <div class="comp-status full">充足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🔵</div>
            <div class="comp-label">A2 抗凝血</div>
            <div class="comp-status low">不足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🟢</div>
            <div class="comp-label">B1 降血糖</div>
            <div class="comp-status full">充足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🟡</div>
            <div class="comp-label">B2 維生素</div>
            <div class="comp-status low">不足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🟣</div>
            <div class="comp-label">C1 降血脂</div>
            <div class="comp-status full">充足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">⚪</div>
            <div class="comp-label">C2 助眠</div>
            <div class="comp-status full">充足</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🔶</div>
            <div class="comp-label">D1 備用</div>
            <div class="comp-status empty">空</div>
          </div>
          <div class="compartment">
            <div class="comp-icon">🔷</div>
            <div class="comp-label">D2 備用</div>
            <div class="comp-status empty">空</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Row -->
  <div class="content-grid">
    <!-- Medicine List -->
    <div class="card">
      <div class="card-header">
        <h3>常用藥品</h3>
        <span class="card-action">管理藥品 →</span>
      </div>
      <div class="card-body">
        <div class="medicine-grid">
          <div class="med-card">
            <div class="med-color" style="background:#ea4335"></div>
            <div class="med-info">
              <div class="med-name">諾華 降血壓藥</div>
              <div class="med-dosage">50mg · 每日1次</div>
            </div>
            <div class="med-count">剩 28 錠</div>
          </div>
          <div class="med-card">
            <div class="med-color" style="background:#1a73e8"></div>
            <div class="med-info">
              <div class="med-name">拜瑞妥 抗凝血劑</div>
              <div class="med-dosage">20mg · 每日1次</div>
            </div>
            <div class="med-count">剩 14 錠</div>
          </div>
          <div class="med-card">
            <div class="med-color" style="background:#34a853"></div>
            <div class="med-info">
              <div class="med-name">糖漸平 降血糖藥</div>
              <div class="med-dosage">500mg · 每日2次</div>
            </div>
            <div class="med-count">剩 42 錠</div>
          </div>
          <div class="med-card">
            <div class="med-color" style="background:#fbbc04"></div>
            <div class="med-info">
              <div class="med-name">鈣穩 維生素D3</div>
              <div class="med-dosage">800IU · 每日1次</div>
            </div>
            <div class="med-count">剩 56 錠</div>
          </div>
          <div class="med-card">
            <div class="med-color" style="background:#9c27b0"></div>
            <div class="med-info">
              <div class="med-name">立普妥 降血脂藥</div>
              <div class="med-dosage">10mg · 每日1次</div>
            </div>
            <div class="med-count">剩 21 錠</div>
          </div>
          <div class="med-card">
            <div class="med-color" style="background:#607d8b"></div>
            <div class="med-info">
              <div class="med-name">悠樂丁 助眠藥</div>
              <div class="med-dosage">2mg · 每日0.5錠</div>
            </div>
            <div class="med-count">剩 7 錠</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Health Insights -->
    <div class="card">
      <div class="card-header">
        <h3>健康指標</h3>
        <span class="card-action">完整報告 →</span>
      </div>
      <div class="card-body">
        <div class="metrics-row">
          <div class="metric">
            <div class="metric-value">127/82</div>
            <div class="metric-label">血壓 (mmHg)</div>
          </div>
          <div class="metric">
            <div class="metric-value">72</div>
            <div class="metric-label">心率 (bpm)</div>
          </div>
          <div class="metric">
            <div class="metric-value">98.6</div>
            <div class="metric-label">體溫 (°F)</div>
          </div>
        </div>
        <div style="font-size:12px;color:var(--gray-600);margin-bottom:8px;font-weight:500">本週服藥遵從率</div>
        <div class="weekly-chart">
          <div class="bar-wrapper">
            <div class="bar" style="height:90%"></div>
            <span class="bar-label">一</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:75%"></div>
            <span class="bar-label">二</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:100%"></div>
            <span class="bar-label">三</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:85%"></div>
            <span class="bar-label">四</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:95%"></div>
            <span class="bar-label">五</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:60%"></div>
            <span class="bar-label">六</span>
          </div>
          <div class="bar-wrapper">
            <div class="bar" style="height:80%"></div>
            <span class="bar-label">日</span>
          </div>
        </div>
        <div style="display:flex;justify-content:space-between;font-size:11px;color:var(--gray-500);margin-top:8px">
          <span>本週平均 83.6%</span>
          <span style="color:var(--success)">↑ 較上週 +5.2%</span>
        </div>
      </div>
    </div>
  </div>
</main>

<script>
function handleLogin() {
  const screen = document.getElementById('loginScreen');
  const app = document.querySelector('.app');
  screen.classList.add('hidden');
  document.querySelector('.app').classList.add('active');
}

function handleLogout() {
  document.querySelector('.app').classList.remove('active');
  document.getElementById('loginScreen').classList.remove('hidden');
}

function toggleSidebar() {
  document.getElementById('sidebar').classList.toggle('open');
  document.getElementById('overlay').classList.toggle('show');
}

function navigate(el) {
  document.querySelectorAll('.nav-item').forEach(i => i.classList.remove('active'));
  el.classList.add('active');
  if (window.innerWidth <= 768) toggleSidebar();
}

function handleNotification() {
  alert('您有 3 則未讀通知\n• 14:30 藥盒 C1 即將用盡\n• 12:00 用藥已按時服用\n• 08:00 健康數據已同步');
}

function updateDate() {
  const now = new Date();
  const days = ['日', '一', '二', '三', '四', '五', '六'];
  const month = String(now.getMonth() + 1).padStart(2, '0');
  const date = String(now.getDate()).padStart(2, '0');
  document.getElementById('dateDisplay').textContent =
    `${now.getFullYear()}年${month}月${date}日 星期${days[now.getDay()]}`;
}

updateDate();
</script>

</body>
</html>
